# ---------------------------------------------------------------------------
# Chain / Network
# ---------------------------------------------------------------------------
# Chain ID for the target network. Defaults to 8453 (Base Mainnet).
# Valid options:
#   8453  — Base Mainnet
#   84532 — Base Sepolia (testnet)
#   1     — Ethereum Mainnet
#   11155111 — Ethereum Sepolia (testnet)
NEXT_PUBLIC_CHAIN_ID=8453

# Whether new users get all mainnet chains enabled by default.
# When "false" (default), new users only get testnet chains enabled.
DEFAULT_MAINNET_CHAINS_ENABLED=false

# ---------------------------------------------------------------------------
# WalletConnect / Reown AppKit
# ---------------------------------------------------------------------------
# Project ID for WalletConnect integration (required for wallet connection).
# Get yours at: https://dashboard.reown.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# ---------------------------------------------------------------------------
# Pimlico (Bundler / Paymaster)
# ---------------------------------------------------------------------------
# API key for the Pimlico bundler and paymaster service (required for smart account operations).
# Used to submit UserOperations for session key authorization and smart account deployment.
# Get yours at: https://dashboard.pimlico.io
PIMLICO_API_KEY=

# ---------------------------------------------------------------------------
# Hot Wallet Encryption (Legacy)
# ---------------------------------------------------------------------------
# 256-bit encryption key for hot wallet private key storage.
# Only required if migrating from hot wallet to smart account system.
# Must be a 64-character hex string (32 bytes).
# Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
HOT_WALLET_ENCRYPTION_KEY=

# ---------------------------------------------------------------------------
# NextAuth Session
# ---------------------------------------------------------------------------
# Secret key for NextAuth session encryption (required).
# Use a strong random string (at least 32 characters).
# Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=

# The canonical URL of your site. Required in production.
# Example: https://your-domain.com
NEXTAUTH_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# Session Key Policy Limits
# ---------------------------------------------------------------------------
# Maximum spend per transaction in USDC micro-units (6 decimals).
# Example: 1000000000000 = 1,000,000 USDC
SESSION_KEY_MAX_SPEND_PER_TX=1000000000000

# Maximum daily spend in USDC micro-units (6 decimals).
# Example: 10000000000000 = 10,000,000 USDC
SESSION_KEY_MAX_SPEND_DAILY=10000000000000

# Maximum session key lifetime in days.
SESSION_KEY_MAX_EXPIRY_DAYS=365

# Default session key expiry in days when not explicitly specified.
SESSION_KEY_DEFAULT_EXPIRY_DAYS=30

# ---------------------------------------------------------------------------
# Database (MongoDB)
# ---------------------------------------------------------------------------
# MongoDB connection string. Required.
# Example: mongodb://localhost:27017/brevet
# Atlas: mongodb+srv://user:password@cluster.mongodb.net/brevet
MONGODB_URI=mongodb://localhost:27017/brevet
