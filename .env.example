# WalletConnect / Reown AppKit
# Get your project ID from https://dashboard.reown.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Alchemy RPC provider (optional, for custom RPC)
NEXT_PUBLIC_ALCHEMY_ID=

# Base RPC URL (defaults to public Base RPC if not set)
RPC_URL=

# Hot wallet encryption key (32 bytes as 64-char hex string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
HOT_WALLET_ENCRYPTION_KEY=
